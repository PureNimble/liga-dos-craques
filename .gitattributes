# Normaliza fins-de-linha para LF no repositório (evita problemas em CI Linux
# e em scripts shell dentro dos workflows). Editores no Windows lidam com isto.
* text=auto eol=lf

# Binários — não tocar.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.svg text eol=lf
*.ico binary
